#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
     int n;
    scanf("%d",&n);
     if(n%10>=5)
     {
     n=n+(10-n%10);
     printf("%d",n);
     }
    else
    {
     n=(n/10)*10;
        printf("%d",n);
    }
 return 0;
}
