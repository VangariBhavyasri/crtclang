#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int a,s=0;
    scanf("%d",&a);
    while(a!=-1)
    {
        s=s+a;
    scanf("%d",&a);
}
printf("%d",s);
 return 0;
}
